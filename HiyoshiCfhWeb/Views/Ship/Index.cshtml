@model HiyoshiCfhWeb.Models.ShipInfo

@{
    ViewBag.Title = Model.Name;
}

<h2>@Html.DisplayFor(model => model.ShipType.Name) @Model.Name</h2>

<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            速力
        </dt>

        <dd>
            @Html.DisplayFor(model => model.ShipSpeed)
        </dd>

        <dt>
            次の改装レベル
        </dt>

        <dd>
            @if (Model.NextRemodelingLevel != null)
            {
                Html.DisplayFor(modelItem => Model.NextRemodelingLevel);
            }
            else
            {
                <text>改装完了</text>
            }
        </dd>

    </dl>
</div>
